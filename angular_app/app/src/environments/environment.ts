// This file can be replaced during build by using the `fileReplacements` array.
// `ng build` replaces `environment.ts` with `environment.prod.ts`.
// The list of file replacements can be found in `angular.json`.

export const environment = {
  production: false,
  API_HOST: "https://azurefunctionsintegration-apim.azure-api.net/azurefunctionsintegration/",
  OCIM_APIM_SUBSCRIPTION_KEY: "3df1007dcd684fbb84feabde198e3257",
  REGISTRATION_ID: "ap_ident",
  MODEL_ID: "dtmi:modelDefinition:b1lbsuofh:jdtjzftdr2",
  ID_SCOPE: "0ne00430809",
  SYMMETRIC_KEY: "EJLLVQjikGtql4u2jpDgP1Ul1nYlLQ9smWjs610R7mY="
};

/*
 * For easier debugging in development mode, you can import the following file
 * to ignore zone related error stack frames such as `zone.run`, `zoneDelegate.invokeTask`.
 *
 * This import should be commented out in production mode because it will have a negative impact
 * on performance if an error is thrown.
 */
// import 'zone.js/plugins/zone-error';  // Included with Angular CLI.
